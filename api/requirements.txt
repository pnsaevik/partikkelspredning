azure-functions

# TEMPORARY: trimmed to the bare minimum for the function_app.py hello-world
# diagnostic (see that file's docstring and CHANGELOG.md's [0.1.4] entry).
# The full app's dependencies - azure-data-tables, azure-storage-queue,
# azure-storage-blob, and partikkelspredning itself (installed from a wheel
# vendored by build_vendor_wheel.sh) - are restored along with
# function_app.py when this diagnostic is reverted.
