"""Static data package: only `definitions.json` lives here, no Python logic.

Loaded by `partikkelspredning.services.form_registry.load_forms` via
`importlib.resources`, so it doesn't need an entry in the layer import
table in CLAUDE.md's "Architecture" section - it's data, not code.
"""
