name: Test package

on: workflow_call

jobs:
  pytest:
    runs-on: ubuntu-latest
    name: Run pytest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - run: pip install -e ".[dev]"

      - run: pytest
